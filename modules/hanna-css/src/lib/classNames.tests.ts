import { htmlCl } from './classNames.js';

if (false as boolean) {
  /* eslint-disable @typescript-eslint/no-unused-vars */
  const exports2: Record<keyof typeof htmlCl, true> = {
    menuIsActive: true,
    menuIsClosed: true,
    menuIsOpen: true,
    beforeSprinkling: true,
  };
  /* eslint-enable */
}
