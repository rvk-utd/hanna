{{ attach_library('hanna/FieldGroup') }}

{% set smallClass = small is defined and small ?
    ? ' FieldGroup--small'
    : ''
%}

<fieldset class="FieldGroup{{smallClass}}">
  <legend>{{ legend }}</legend>
	{% block content %}
		{{ content }}
	{% endblock %}
</fieldset>
