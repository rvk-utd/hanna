{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // somehow setting this in "../../tsconfig.base.json" doesn't work
    "jsx": "react"
  },
  "include": ["../**/*.ts", "../**/*.tsx"]
}
